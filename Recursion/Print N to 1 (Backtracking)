#include <bits/stdc++.h>
using namespace std;

int i = 1;

void printN(int i, int n) {
    if (n == 0) {
        return;
    }
    printN(i + 1, n - 1);
    cout << i << "\n";
}

int main() {

    int n;
    cin >> n;
    printN(i, n);

}
